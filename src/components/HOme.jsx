import React, { useState } from 'react'
import Login from './Auth/Login'




const HOme = () => {

  
  
  
  return (
    <>
    

    
    </>
  )
}

export default HOme
 {/* <div className="container">
        {!user ? <HOme handleLogin={handleLogin} /> : null}
        {user === "admin" ? (
          <AdminDashboard changeUser={setUser} data={{ firstName: "Admin" }} />
        ) : user === "employee" ? (
          <EmployeeDashboard changeUser={setUser} data={loggedUser} />
        ) : null}
      </div> */}
   {/* <Routes>
      <Route
        path="/"
        element={<Login handleLogin={props.handleLogin} />}
      />
      <Route
        path="/newAcc"
        element={<NewAcc />}
      />
    </Routes> */}